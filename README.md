# Meilisearch and Grafbase example

To run locally with an index called `movies`, set the following environment variable:

```
MEILISEARCH_API_URL=http://localhost:7700
MEILISEARCH_API_KEY=<master-key-goes-here>
```

